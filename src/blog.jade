include ./page.mixin.jade

link(rel="stylesheet" href="blog.css")

script(src="https://cdn.jsdelivr.net/highlight.js/9.1.0/highlight.min.js")

//- Yep, this is a Mithril app.
script(src=(
    "https://cdnjs.cloudflare.com/ajax/libs/mithril/0.2.2-rc.1/mithril" +
        (minified ? ".min" : "") + ".js"
))

//- And, best to pull in Marked this way.
script(src=(
    "https://cdnjs.cloudflare.com/ajax/libs/marked/0.3.5/marked" +
        (minified ? ".min" : "") + ".js"
))

script(src="blog.js")

+page("Blog")
    //- So the blog wrapper is separate from the page wrapper.
    #blog
        .info
            #info: noscript You need JavaScript enabled for this to work.
            .license-divider
