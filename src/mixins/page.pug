doctype html
meta(charset="utf-8")
meta(name="viewport" content="width=device-width,initial-scale=1")
meta(name="google-site-verification" content="uV7Lko0q31ot2kgRjnMeU4sN5Fx4Whld80ItNCYYkA4")
//- NOTE: this *must* be kept in sync with `$color-main` in `common.styl`.
meta(name="theme-color" content="#942a39")
link(rel="license" href="https://github.com/isiahmeadows/website/blob/gh-pages/LICENSE")
link(rel="stylesheet" href="/page.css")
link(rel="stylesheet" href=pageCSS)
link(rel="shortcut icon" href="/favicon.ico")

//- page(name, title)
//- page(name, title).prefer-name
mixin page(name, title)
    - assert(name, "(mixin page) Page `name` required!")
    - var header = name
    - if (title && /[\s^]prefer-name[\s$]/.test(attributes.class)) header = title
    title Isiah Meadows - #{title ? name + " - " + title : name}

    #content
        #header
            nav
                div
                    a(href="/") Isiah Meadows

                div
                    .link.l1: a&attributes(navAttrs("/about/")) About
                    .link.l2: a&attributes(navAttrs("/showcase/")) Showcase
                    .link.l3: a&attributes(navAttrs("/blog/")) Blog
                    .link.l4: a&attributes(navAttrs("/contact/")) Contact

        //- Hack for 2-column layout
        #content-body
            #body-wrapper: div
                h2(class=pageColor)= header
                #body: block

            #license: div
                footer: small
                    p.
                        This site does use Google Analytics for basic tracking
                        (IP addresses anonymized), and a few third parties
                        collect a few cookies their own, like GitHub and some of
                        the creators of the widgets in the #[a(href="/about/") About]
                        page's widgets. If you don't consent to these, either
                        stop using this site or just block them in some way,
                        like with a firewall or with a blocker like
                        #[a(href="https://adblockplus.org/") Adblock Plus],
                        #[a(href="https://github.com/gorhill/uBlock") uBlock Origin].

                    p.
                        All content available on this site by Isiah Meadows is
                        licensed under the Creative Commons Attribution 4.0
                        International License, and all source code is licensed
                        under the ISC License, unless otherwise specified.

                    p: a(
                        href="https://github.com/isiahmeadows/website/blob/gh-pages/LICENSE"
                        target="_blank" rel="license"
                    ) See here for the text of both of these licenses.


    include ./analytics.pug
